
@{
    ViewBag.Title = "首页产品图片录入";
}
@using OHYModel;
@model List<IndexImage>
<link href="~/Content/css/ohyStyle.css" rel="stylesheet" />
<h2>@ViewBag.Title</h2>
<section>
    <ul>
        <li>
            <label>语言：</label>
            <select id="language" name="language">
                <option @(LanguageEnum.中文 == Language ? "selected" : "") value="@LanguageEnum.中文">@LanguageEnum.中文</option>
                <option @(LanguageEnum.English == Language ? "selected" : "") value="@LanguageEnum.English">@LanguageEnum.English</option>
            </select>
            <a href="/IndexImageAdd/index">新增</a>
        </li>

    </ul>
    <ul>
        <li>
            @foreach (var par in Model)
            {
                @*<li>
                        <label>轮播图：</label><img src="@par.file" style="width:40%;height:60px;"/>
                        <a href="/IndexImageUpdate/index?id=@par._id">修改</a>
                    </li>*@

                //var name = par.Name == "" ? "暂无名字" : par.Name;
                <a href="/IndexImageUpdate/index?id=@par._id">
                    <img src="@par.file" />
                    @*<label>图片名字:@(par.Name == null ? "暂无名字" : @par.Name)</label>*@
                    @*<p>@(par.Name == null ? "暂无名字" : @par.Name)</p>*@
                </a>
                


            }
        </li>
    </ul>

</section>
@Html.Partial("PageView");
<script>
    $("#language").change(function () {
        var lan = $("#language").val();
        window.location = "/IndexImageList/index?language=" + lan;
    });
</script>

